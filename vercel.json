{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "functions": {
    "api/marker-submit.js": { "maxDuration": 60 },
    "api/marker-result.js": { "maxDuration": 60 }
  }
}
